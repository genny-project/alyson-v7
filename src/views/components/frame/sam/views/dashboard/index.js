import React from 'react';
import { Typography } from '@material-ui/core';
// const getDashboardItems = frames =>

const Dashboard = ({ frames, asks }) => {
  return <Typography>{'dashboard'}</Typography>;
};

export default Dashboard;
